# Supabase project configuration

[functions.stripe-webhook]
verify_jwt = false

# Invoked by pg_cron/pg_net with a shared-secret header instead of a user JWT.
[functions.reconcile-stripe-checkouts]
verify_jwt = false
